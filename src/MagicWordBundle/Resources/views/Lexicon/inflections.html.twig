<ul class="list-group" style="max-height:500px; overflow-y:scroll">
    {% for inflection in inflections %}
        <li class="list-group-item">{{ inflection.content }} ({{ inflection.lemma.content }} [{{ inflection.lemma.category ? inflection.lemma.category.value : "" }}])</li>
    {% endfor %}
</ul>
