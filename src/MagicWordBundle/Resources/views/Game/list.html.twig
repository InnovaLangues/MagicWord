{% extends '::base.html.twig' %}

{% block title %}Liste des parties{% endblock %}

{% block body -%}
    <div class="panel">
        <div class="panel-body">
            <a class="btn btn-default btn-sm" href="{{ path('train') }}">S'entrainer</a>
            <a class="btn btn-default btn-sm" href="{{ path('challenge') }}">Défier quelqu'un</a>

            <h3>liste des parties massives</h3>
            <ul>
                {% for game in games if game.discr == 'massive' %}
                    <li>
                        <a href="">{{ game.id }} - {{ game.discr }}</a>
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}
