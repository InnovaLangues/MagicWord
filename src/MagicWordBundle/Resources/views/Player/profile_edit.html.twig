{% extends '::base.html.twig' %}

{% block body -%}
<div class="panel panel-default">
    <div class="panel-heading">
        <h4>
            {% include "MagicWordBundle:Common:backtohome.html.twig" %}
            {{ 'edit_profile' | trans }}
        </h4>
    </div>
    <div class="panel-body">
        {{ form_start(form) }}
        <div class="form-group">
            {{ form_row(form.profileText) }}
        </div>

        <div class="form-group">
            {{ form_label(form.profilePicFile) }}
            {{ form_errors(form.profilePicFile) }}
            {% if app.user.profilepicPath %}
            <div>
                <img height="200px" src="{{ asset('upload/profilepic/' ~ app.user.profilepicPath) }}"/>
            </div>
            {% endif %}
            {{ form_widget(form.profilePicFile) }}
        </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
